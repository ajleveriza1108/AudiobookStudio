\
# Audiobook Studio R1.17.7.1 — Complete One-Click Fix

This cumulative update preserves the RTX 2050 GPU runtime while installing the
exact selected-PDF and persistent ebook-removal corrections.

Extract the complete ZIP, then double-click:

```text
INSTALL_R1_17_7_1_COMPLETE_FIX.bat
```

Default project:

```text
D:\Python\AudiobookStudio
```

The installer preserves `.venv`, `.gpu-venv`, `.advanced-ocr-venv`, books,
PDFs, audio, projects, models, voices, outputs, and local user state.
