# Install Audiobook Studio R1.17.7.4

Run the package-level `INSTALL_R1_17_7_4_FULL_SPORTS_FIX.bat` after closing the
application. The installer creates a rollback backup, rebuilds stale OCR caches,
verifies installed hashes, and runs Windows GUI smoke checks.
